---
title: "Version 1.0.101 - CVE Reference Links Open in Multiple Popups"
date: "2025-10-22"
version: "1.0.101"
status: "Released"
category: "Bug Fix"
---

# Version 1.0.101 - CVE Reference Links Open in Multiple Popups

**Release Status**: Released
**Release Date**: 2025-10-22
**Category**: Bug Fix
**Issue**: [HEX-314](https://linear.app/hextrackr/issue/HEX-314)

## Overview

Fixed CVE reference links in the Vulnerability Details Modal to open in separate popup windows instead of reusing the same window. This enables users to compare multiple security advisories (CVE.org, NIST NVD, vendor advisories) side-by-side during vulnerability research.

## Fixed

### CVE Reference Links Reusing Same Popup Window (HEX-314)

**Problem**:
- Clicking any CVE reference link opened in a 1200x1200 popup window
- Clicking a second reference link replaced the first popup's content
- Users could not compare multiple advisories side-by-side
- Expected: Each link opens its own popup for parallel comparison

**Root Cause**:
- `window.open()` was using a named window target `'cve_ref_popup'`
- Per W3C/WHATWG specification, named targets reuse existing windows with that name
- All reference links shared the same target name, causing window reuse

**Solution**:
- Changed window target from `'cve_ref_popup'` to `'_blank'`
- `'_blank'` forces each link to open in a new browsing context
- Window features (`width=1200,height=1200`) maintain popup behavior

**File Modified**:
- `app/public/scripts/shared/vulnerability-details-modal.js:234` - One-word change

**Code Change**:
```diff
- onclick="window.open('${sanitizedUrl}', 'cve_ref_popup', 'width=1200,height=1200,scrollbars=yes,resizable=yes'); return false;"
+ onclick="window.open('${sanitizedUrl}', '_blank', 'width=1200,height=1200,scrollbars=yes,resizable=yes'); return false;"
```

## User Experience Improvements

**Before Fix**:
1. Click "CVE.org Official Record" → Opens popup
2. Click "NIST NVD" → Replaces first popup content (CVE.org closes)
3. Cannot compare multiple sources simultaneously

**After Fix**:
1. Click "CVE.org Official Record" → Opens first popup
2. Click "NIST NVD" → Opens second popup (first remains open)
3. Click vendor advisory → Opens third popup (both previous remain open)
4. All popups maintain 1200x1200 dimensions for optimal viewing
5. Users can compare multiple authoritative sources side-by-side

**Use Case**: Security researchers need to cross-reference CVE.org official records, NIST NVD analysis, and vendor-specific advisories simultaneously to understand vulnerability scope, affected versions, and remediation strategies.

## Technical Details

**window.open() Target Parameter Behavior**:
- **Named target** (e.g., `'cve_ref_popup'`): Reuses existing window with that name
- **`'_blank'`**: Always creates new browsing context
- **Window features**: `width=1200,height=1200,scrollbars=yes,resizable=yes` forces popup vs tab

**Browser Compatibility**: This is standard W3C behavior supported by all modern browsers (Chrome, Firefox, Safari, Edge).

## Validation

- ✅ CVE.org Official Record opens in new popup
- ✅ NIST NVD opens in separate popup (first remains open)
- ✅ Vendor advisory links each open in new popups
- ✅ All popups maintain 1200x1200 dimensions
- ✅ Multiple popups can be arranged side-by-side
- ✅ No regression in popup functionality
- ✅ Dark/light theme compatibility verified
- ✅ No console errors or warnings

## Documentation Improvements

- **JSDoc**: Fixed documentation for `findDocsSectionForFilename` in `app/controllers/docsController.js` ([DOCS-56](https://linear.app/hextrackr/issue/DOCS-56))
- **Content**: Fixed inaccurate controller count in `architecture/backend.md` - Updated from 11 to 13 controllers, added missing `locationController.js` and `auditLogController.js` documentation ([DOCS-57](https://linear.app/hextrackr/issue/DOCS-57))
- **Content**: Fixed three inaccuracies in `architecture/backend.md` - Updated `server.js` line count from ~557 to ~831 lines, corrected service module count from 18 to 20, and corrected route module count from 12 to 15 ([DOCS-58](https://linear.app/hextrackr/issue/DOCS-58))
- **JSDoc**: Fixed documentation for `getStats` in `app/controllers/docsController.js` - Added @async, @returns, @throws, and @example tags for complete API documentation ([DOCS-59](https://linear.app/hextrackr/issue/DOCS-59))
- **Content**: Fixed DocsController pattern incorrectly documented as "Constructor pattern" in `architecture/backend.md` - Corrected to "Module Exports" pattern with new dedicated section explaining the hybrid internal instance approach ([DOCS-60](https://linear.app/hextrackr/issue/DOCS-60))
- **JSDoc**: Fixed documentation for `getAddressSuggestions` in `app/controllers/ticketController.js` - Added @async, @param, @returns, @throws, and @example tags for complete API documentation of address autofill endpoint ([DOCS-61](https://linear.app/hextrackr/issue/DOCS-61))
- **Content**: Fixed incomplete DocsController pattern example in `architecture/backend.md` - Added missing try-catch error handling with console.error logging and custom error message that exists in actual implementation ([DOCS-62](https://linear.app/hextrackr/issue/DOCS-62))

## Code Quality Improvements

- **Cleanup**: Removed debug console.log from `app/public/scripts/shared/location-details-modal.js:1062` ([HEX-317](https://linear.app/hextrackr/issue/HEX-317))
- **Cleanup**: Replaced all console logging with structured logger in `app/controllers/kevController.js` - Upgraded 7 error handlers from `console.error()` to `global.logger.kev.error()` with structured metadata (error message, stack trace, source context) for consistency with unified logging system ([HEX-322](https://linear.app/hextrackr/issue/HEX-322))

---

**Sprint**: HEX-314 Bug Fix
**Impact**: Low-risk, one-word change with high UX improvement
**Files Changed**: 1 file, 1 line modified
