# HexTrackr v6.66 - Node.js Application Docker Setup

services:
  hextrackr:
    build: .
    container_name: hextrackr-app
    ports:
      - "3040:3232"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=production
    restart: unless-stopped
