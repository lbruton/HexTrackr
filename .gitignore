# Dependencies
node_modules/
npm-debug.log*

# Environment variables (SECRETS - never commit)
.env
.env.local
.env.production

# SSL Certificates (SECRETS - never commit)
certs/
*.pem
*.key
*.crt
*.cert

# Secrets directory (Docker secrets, API keys)
secrets/

# Database files
data/*.db
data/*.sqlite
*.db
*.sqlite
*.db-shm
*.db-wal

# Application database directory (HEX-120)
app/public/data/
!app/public/data/.gitkeep

# Database backups (too large for GitHub)
app/data/*.backup*
app/data/*.bak

# Deprecated database files (corrupt/recovery files)
app/data/deprecated/

# Log files
logs/
*.log

# Agent logs (preserved locally but not committed)
/hextrackr-specs/data/agentlogs/

# Developer documentation (auto-generated)
app/dev-docs-html/

# Temporary files
tmp/
temp/

# Prime context handoff file (session-specific)
prime_handoff.json

# Playwright test artifacts
.playwright-mcp/
test-results/

# Uploads (binary blobs - too large for GitHub)
uploads/
app/uploads/
app/public/uploads/

# CSV files (keep smaller test files)
*vulnerabilities*.csv
!test-import.csv

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo

# Backup files (timestamped backups of source files)
*.backup
*.backup.*
*.bak

# Archive files (too large for GitHub)
*.zip

# ============================================
# AI Tool Configurations (local dev only)
# ============================================

# Claude Code (session-specific config, agents, commands)
.claude/

# Cursor IDE rules
.cursor/

# Crush AI tool
.crush/

# MCP configuration (contains API keys - SECRETS)
.mcp.json
.mcp*.json.zip

# OpenCode configuration (may contain API keys)
opencode.json
opencode-config-update.json
broken_opencode.json
.opencode.json

# Claude session rewind logs
.rewindlog_*

# Claude agent description (local dev reference)
AGENTS.md

# Codacy AI rules (already handled by .codacy/.gitignore)
.github/instructions/codacy.instructions.md

# rMemory
.rMemory

# ============================================
# Project-specific ignores
# ============================================

# Stooge analysis reports (temporary outputs)
stooges/

# Athena conversation logs (keep directory structure)
claudelogs/sessions/*.md
!claudelogs/sessions/.gitkeep

# Claudelogs embeddings (57-81 MB files)
claudelogs/embeddings/

# Memento shared database
memento/

# Deprecated scripts directory
depreciated/

# Development environment
docker-compose.dev.yml
scripts/start-dev-env.sh
scripts/stop-dev-env.sh

# External MCP Tools (FilScope)
filescope-tools-demo.js
FileScopeMCP-*.json
FileScopeMCP-*.html
FileScopeMCP-*.png

# Test artifacts and coverage reports
__tests__/coverage/
__tests__/test-results/
__tests__/playwright-report/
__tests__/test-results.json
**/*.lcov

# Backups directory
backups/

# Utility scripts with potential secrets (use npm scripts instead)
app/scripts/
!app/scripts/.gitkeep

# Duplicate Dockerfiles (keep only primary)
Dockerfile.node.backup
