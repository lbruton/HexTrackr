# Dependencies
node_modules/
npm-debug.log*

# Environment variables
.env
.env.local
.env.production

# Database files
data/*.db
data/*.sqlite
*.db
*.sqlite

# Log files
logs/
*.log

# Agent logs (preserved locally but not committed)
/hextrackr-specs/data/agentlogs/

# Developer documentation (auto-generated)
app/dev-docs-html/

# Temporary files
tmp/
temp/

# Playwright test artifacts
.playwright-mcp/
test-results/

# CSV uploads (keep sample files)
uploads/*.csv
!uploads/sample*.csv
!uploads/test*.csv

# Large CSV files (keep smaller test files)
*vulnerabilities*.csv
!test-import.csv

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/settings.json
.idea/
*.swp
*.swo

# Backup files
*.backup
*.bak

# Development environment (exclude from public pushes)
docker-compose.dev.yml
scripts/start-dev-env.sh
scripts/stop-dev-env.sh

# External MCP Tools (FilScope)
filescope-tools-demo.js
FileScopeMCP-*.json
FileScopeMCP-*.html
FileScopeMCP-*.png

# Test artifacts and coverage reports
__tests__/coverage/
__tests__/test-results/
__tests__/playwright-report/
__tests__/test-results.json
**/*.lcov

#Ignore vscode AI rules
.github/instructions/codacy.instructions.md
.rMemory

# Stooge analysis reports (temporary outputs)
stooges/

# Athena's Library of Sacred Scrolls
# Keep the directory structure but not the actual conversation logs
claudelogs/sessions/*.md
!claudelogs/sessions/.gitkeep

# Athena tracking file (preserve to avoid reprocessing)
# scripts/.athena-processed-scrolls.json

# Memento shared database
memento/
-e 
# OpenCode configuration (may contain API keys)
opencode.json
broken_opencode.json
.opencode.json
